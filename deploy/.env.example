# Publish Gateway execution route before switching MCP to it.
CCDB_MCP_RESOURCE=https://gateway-base-test.carbonstop.com/mcp/ccdb
CCDB_MCP_EXECUTION_URL=https://gateway-base-test.carbonstop.com/internal/ccdb/mcp/execute
CCDB_MCP_ALLOWED_HOSTS=ccdb-mcp:3400,127.0.0.1:3400
CCDB_MCP_ALLOWED_ORIGINS=
# Inject CCDB_MCP_CONTEXT_KEYS using the deployment Secret system.
# No real keys in Git or chat. Template intentionally requires a private key configuration.
